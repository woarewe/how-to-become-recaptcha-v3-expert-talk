<%= form_with(model: contact_inquiry) do |form| %>
  <% if contact_inquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_inquiry.errors.count, "error") %> prohibited this contact_inquiry from being saved:</h2>

      <ul>
        <% contact_inquiry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <%= form.hidden_field :recaptcha_token, data: { "recaptcha-site-key" => ReCaptchaValidator::SITE_KEY } %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
